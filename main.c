#include <stdio.h>

#include "Operators.h"
#include "Interface.h"

int main(int argc, char const *argv[]){
    UINT32 test = 0;
    BOOLEAN valid_flag;
    // test = sum(4,5,&valid_flag);

    return 0;
}
